name: React TypeScript CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - name: Checkout code to parent directory
      run: |
        # Navigate to the home directory
        cd /home/ubuntu
        # Clone the repository directly into the home directory
        git clone https://github.com/Nitin-Vected/Support-System-Frontend.git

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: Install dependencies
      run: |
        cd /home/ubuntu/Support-System-Frontend  # Navigate to the project directory
        npm install

    - name: Build the project
      run: |
        cd /home/ubuntu/Support-System-Frontend  # Navigate to the project directory
        npm run build --if-present

    - name: Test Nginx Configuration
      run: |
        sudo nginx -t

    - name: Restart Nginx
      run: |
        sudo systemctl restart nginx
